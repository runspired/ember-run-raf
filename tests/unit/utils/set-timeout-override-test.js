import setTimeoutOverride from 'ember-run-raf/utils/set-timeout-override';

import { module, test } from 'qunit';

module('Unit | Utility | set timeout override', function() {
  // Replace this with your real tests.
  test('Module Exists', function(assert) {
    assert.ok(setTimeoutOverride);
  });
});